# `djangordf.manager`

The `RDFManager` attached to every `RDFModel` subclass as `objects`,
and the lazy `RDFQuerySet` returned by `all()` and `filter()`.

```{eval-rst}
.. automodule:: djangordf.manager
   :members:
   :show-inheritance:
```
