# `djangordf.skos`

The SKOS class IRI constants, the convention map (`DEFAULT_PREDICATES`)
used by the metaclass to assign implicit predicates, and the
`resolve_curie` shortcut.

```{eval-rst}
.. automodule:: djangordf.skos
   :members:
```
