# `djangordf.loaders`

Read-only loaders for external SKOS vocabularies. Pull GND, AAT,
Wikidata, or any other RDF dump into the configured triple store
under a dedicated external graph.

```{eval-rst}
.. automodule:: djangordf.loaders
   :members:
```
